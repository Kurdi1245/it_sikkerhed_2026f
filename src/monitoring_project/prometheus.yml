global:
  scrape_interval: 15s       # Henter data hvert 15. sekund
  evaluation_interval: 15s

scrape_configs:
  - job_name: "fastapi-app"
    static_configs:
      - targets: ["app:8000"]   # 'app' er service-navnet i Docker Compose
